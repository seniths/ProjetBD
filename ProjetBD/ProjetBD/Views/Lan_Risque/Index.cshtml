@model IEnumerable<ProjetBD.Lan_Risque>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.texte)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Langue.libelle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Risque.idRisque)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.texte)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Langue.libelle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Risque.idRisque)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idLangueRisque }) |
            @Html.ActionLink("Details", "Details", new { id=item.idLangueRisque }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idLangueRisque })
        </td>
    </tr>
}

</table>
