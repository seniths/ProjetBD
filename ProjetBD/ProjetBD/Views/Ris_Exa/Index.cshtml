@model IEnumerable<ProjetBD.Ris_Exa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Examen.idExamen)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Risque.idRisque)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Examen.idExamen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Risque.idRisque)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRisqueExamen }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRisqueExamen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRisqueExamen })
        </td>
    </tr>
}

</table>
